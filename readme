To turn this source code into an executable, you can use rustc:

```
$ rustc hello.rs
$ ./hello
```

To create new package with `cargo new`:

```$ cargo new hello_world```

build: 
```cargo build```

run:
```cargo run```

check:
```cargo check```

open cargo docs
```cargo doc --open```
